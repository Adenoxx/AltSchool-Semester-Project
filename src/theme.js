import { createTheme } from "@mui/material";

export const theme = createTheme({
      colors: {
      darkModeBgColor: "##141D2F",
      lightModeBgColor: "#FEFEFE",
      searchBgColorMode: "#1E2A47",
    },
});
